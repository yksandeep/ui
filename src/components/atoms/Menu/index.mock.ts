import { IMenu } from ".";

const base: IMenu = {
  indicator: true,
  hideScroll: false,
  indicatorSize: "1px",
  hoverEffect: true,
  scrollable: false,
};

export const mockMenuProps = {
  base,
};
